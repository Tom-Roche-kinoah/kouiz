<!doctype html>
<html lang="fr">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Reset CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <!-- Really beautiful CSS -->
    <!-- <link href="/css/floating-labels.css" rel="stylesheet"> -->
    <!-- <link href="/css/style.css" rel="stylesheet"> -->
    <link href="/css/oquiz.css" rel="stylesheet">

    <title>Kouiz</title>
</head>

<body>
    <header>
        <nav class="navbar fixed-top navbar-expand-md bg-primary">
            <div class="container">
              <a class="navbar-brand logo" href="/">Kouiz</a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">

                    <% if(user) { %>
                      <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                          <%= user.firstname %> <%= user.lastname  %>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                          <li><a class="dropdown-item<% if(page === 'profile') { %> active<% } %>" href="/profile">Mon profil</a></li>
                          <li><a class="dropdown-item<% if(page === '#') { %> active<% } %>" href="/logout">Déconnexion</a></li>

                          <% if(user && user.role === 'admin') { %>
                          <li><hr class="dropdown-divider"></li>
                          <li class="dropdown-item"><strong>Admin</strong></li>
                          <li><a class="dropdown-item<% if(page === 'admin') { %> active<% } %>" href="/admin/users">Gestion des utilisateurs</a></li>
                          <li><a class="dropdown-item<% if(page === 'questions') { %> active<% } %>" href="/admin/questions">Toutes les questions</a></li>
                          <li><a class="dropdown-item<% if(page === 'add-question') { %> active<% } %>" href="/admin/add-question/">Ajouter une question</a></li>
                          <li><a class="dropdown-item" href="#">Dashboard</a></li>
                          <% } %>

                        </ul>
                      </li>
                    <% } %>

                    <li class="nav-item"><a class="nav-link<% if(page === 'home') { %> active<% } %>" href="/">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link<% if(page === 'category') { %> active<% } %>" href="/categories">Sujets</a></li>

                    <% if(!user) { %>
                      <li class="nav-item"><a class="nav-link<% if(page === 'register') { %> active<% } %>" href="/register">S'inscrire</a></li>
                      <li class="nav-item"><a class="nav-link<% if(page === 'login') { %> active<% } %>" href="/login">Se connecter</a></li>
                    <% } %>

                </ul>
              </div>
            </div>
          </nav>

    </header>
    <main class="container">